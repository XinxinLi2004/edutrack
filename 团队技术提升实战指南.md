# 团队技术提升实战指南

> 面向 1-3 人小团队的全栈开发能力体系建设
> 编写：资深开发工程师（Senior Developer）

---

## 目录

1. [技术栈选型建议](#1-技术栈选型建议)
2. [代码质量规范体系](#2-代码质量规范体系)
3. [代码审查机制](#3-代码审查机制)
4. [小团队开发流程](#4-小团队开发流程)
5. [开发者能力成长路径](#5-开发者能力成长路径)
6. [工具链配置指南](#6-工具链配置指南)
7. [项目实战检查清单](#7-项目实战检查清单)

---

## 1. 技术栈选型建议

### 1.1 推荐技术栈：Laravel + Livewire + FluxUI

对于 1-3 人的小团队，我强烈推荐 **Laravel 全栈方案**，原因如下：

| 对比维度 | Laravel 全栈方案 | 前后端分离方案（React+Node） | 传统方案（jQuery+PHP） |
|---------|-----------------|--------------------------|---------------------|
| 学习曲线 | 中等，文档优秀 | 陡峭，需学两套生态 | 低，但天花板也低 |
| 开发效率 | 极高，全栈一人搞定 | 中等，前后端需协调 | 低，重复劳动多 |
| 团队要求 | 1人即可全栈开发 | 至少需前后端各1人 | 1人可做，但难扩展 |
| 维护成本 | 低，单一技术栈 | 高，双倍依赖管理 | 中等，但代码难维护 |
| 生态成熟度 | 非常成熟，10年+ | 成熟但碎片化 | 衰退中 |
| 性能表现 | 优秀（Swoole加速） | 优秀（SSR） | 一般 |

### 1.2 完整技术栈推荐

```
┌─────────────────────────────────────────────┐
│              前端层 (Frontend)                │
│  Tailwind CSS 3.x  │  Alpine.js  │  FluxUI   │
├─────────────────────────────────────────────┤
│              交互层 (Interaction)             │
│         Livewire 3.x  │  Livewire Volt       │
├─────────────────────────────────────────────┤
│              后端层 (Backend)                 │
│    Laravel 11.x  │  PHP 8.3  │  MySQL 8.x   │
├─────────────────────────────────────────────┤
│              基础设施 (Infrastructure)         │
│  Nginx  │  Redis  │  Laravel Octane (Swoole) │
├─────────────────────────────────────────────┤
│              工具链 (Toolchain)               │
│  Git  │  Composer  │  NPM  │  Laravel Pint   │
└─────────────────────────────────────────────┘
```

### 1.3 项目初始化命令

```bash
# 创建新项目
composer create-project laravel/laravel my-project
cd my-project

# 安装 Livewire 和 FluxUI
composer require livewire/livewire livewire/flux

# 安装 Tailwind CSS
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init

# 安装开发工具
composer require --dev laravel/pint larastan/larastan

# 初始化 Git
git init
git add .
git commit -m "feat: 项目初始化"
```

---

## 2. 代码质量规范体系

### 2.1 编码规范核心原则

```
┌──────────────────────────────────────────┐
│           代码质量金字塔                    │
├──────────────────────────────────────────┤
│                                          │
│           ▲ 能跑通                       │  ← 最低标准（不够）  
│          ╱─────────╲                     │
│         ╱  可维护性  ╲                    │  ← 基本要求
│        ╱─────────────╲                   │
│       ╱   可测试性     ╲                  │  ← 进阶要求
│      ╱─────────────────╲                 │
│     ╱    可扩展性       ╲                │  ← 高级要求
│    ╱─────────────────────╲               │
│   ╱     团队一致性        ╲              │  ← 最高标准
│  ╱─────────────────────────╲             │
│                                          │
└──────────────────────────────────────────┘
```

### 2.2 PHP / Laravel 编码规范

#### 命名规范

```php
// ✅ 正确：清晰、一致、符合 PSR-12
class CourseEnrollmentController extends Controller
{
    public function enrollStudent(Request $request, Course $course)
    {
        $validatedData = $request->validate([
            'student_name' => ['required', 'string', 'max:255'],
            'phone_number' => ['required', 'regex:/^1[3-9]\d{9}$/'],
            'course_id'    => ['required', 'exists:courses,id'],
        ]);

        $enrollment = CourseEnrollment::create($validatedData);

        return redirect()
            ->route('courses.show', $course)
            ->with('success', '报名成功！');
    }
}

// ❌ 错误：命名模糊、缩写、不符合规范
class CEC extends Controller {
    public function enroll($req, $c) {
        $d = $req->validate([...]);
        $e = CE::create($d);
        return redirect()->route('courses.show', $c)->with('s', 'ok');
    }
}
```

#### 控制器规范 — 单一职责

```php
// ✅ 控制器只做：接收请求 → 调用业务逻辑 → 返回响应
class CourseController extends Controller
{
    public function __construct(
        private readonly CourseService $courseService,
    ) {}

    public function store(StoreCourseRequest $request): RedirectResponse
    {
        $course = $this->courseService->create($request->validated());

        return redirect()
            ->route('courses.show', $course)
            ->with('success', "课程「{$course->name}」创建成功");
    }

    public function show(Course $course): View
    {
        $course->load(['teacher', 'enrollments.student']);

        return view('courses.show', compact('course'));
    }
}

// 业务逻辑放在 Service 层
class CourseService
{
    public function create(array $data): Course
    {
        return DB::transaction(function () use ($data) {
            $course = Course::create($data);

            // 自动创建对应的课时记录
            $this->createDefaultSessions($course);

            return $course;
        });
    }
}
```

#### 模型规范 — 胖模型瘦控制器 ❌ → 瘦模型 + Service ✅

```php
// ✅ 模型只定义数据结构和关系
class Course extends Model
{
    protected $fillable = [
        'name',
        'description',
        'teacher_id',
        'max_students',
        'start_date',
        'end_date',
        'status',
    ];

    protected $casts = [
        'start_date' => 'date',
        'end_date'   => 'date',
        'max_students' => 'integer',
    ];

    // 关系定义
    public function teacher(): BelongsTo
    {
        return $this->belongsTo(Teacher::class);
    }

    public function enrollments(): HasMany
    {
        return $this->hasMany(CourseEnrollment::class);
    }

    // 查询作用域 — 可复用的查询条件
    public function scopeActive(Builder $query): Builder
    {
        return $query->where('status', 'active')
            ->where('start_date', '<=', now())
            ->where('end_date', '>=', now());
    }

    // 访问器 — 派生属性
    public function getEnrollmentCountAttribute(): int
    {
        return $this->enrollments()->count();
    }

    public function getIsFullAttribute(): bool
    {
        return $this->enrollment_count >= $this->max_students;
    }
}
```

### 2.3 前端编码规范

#### Blade 模板规范

```blade
{{-- ✅ 正确：语义化、组件化、可读性强 --}}
<flux:main class="space-y-6">
    <flux:heading size="xl">{{ $course->name }}</flux:heading>

    <flux:card class="space-y-4">
        <div class="flex items-center justify-between">
            <flux:text variant="strong">授课教师</flux:text>
            <flux:text>{{ $course->teacher->name }}</flux:text>
        </div>

        <div class="flex items-center justify-between">
            <flux:text variant="strong">报名情况</flux:text>
            <flux:badge
                :color="$course->is_full ? 'red' : 'green'"
            >
                {{ $course->enrollment_count }} / {{ $course->max_students }}
            </flux:badge>
        </div>
    </flux:card>

    @if($course->is_full)
        <flux:callout color="red" icon="exclamation-triangle">
            该课程已满员，无法继续报名
        </flux:callout>
    @endif
</flux:main>

{{-- ❌ 错误：逻辑混入视图、缺乏语义 --}}
<div class="container">
    <h1 class="text-3xl font-bold">{{ $c->name }}</h1>
    <?php if($c->enrollments()->count() >= $c->max_students): ?>
        <span style="color:red">满</span>
    <?php endif; ?>
</div>
```

#### JavaScript / Alpine.js 规范

```javascript
// ✅ Alpine 组件：命名清晰、状态明确、逻辑分离
function courseFilter() {
    return {
        // 状态
        search: '',
        category: 'all',
        courses: [],

        // 初始化
        init() {
            this.loadCourses();
        },

        // 计算属性
        get filteredCourses() {
            return this.courses.filter(course => {
                const matchesSearch = course.name
                    .toLowerCase()
                    .includes(this.search.toLowerCase());
                const matchesCategory = this.category === 'all'
                    || course.category === this.category;
                return matchesSearch && matchesCategory;
            });
        },

        // 动作
        async loadCourses() {
            const response = await fetch('/api/courses');
            this.courses = await response.json();
        },

        resetFilters() {
            this.search = '';
            this.category = 'all';
        },
    };
}
```

### 2.4 Git 提交规范

#### 提交信息格式

```
<类型>(<范围>): <简短描述>

<可选的详细说明>

<可选的脚注>
```

| 类型 | 说明 | 示例 |
|------|------|------|
| `feat` | 新功能 | `feat(course): 添加课程报名功能` |
| `fix` | Bug 修复 | `fix(enrollment): 修复满员课程仍可报名的问题` |
| `refactor` | 重构 | `refactor(course): 将业务逻辑迁移到 Service 层` |
| `style` | 样式调整 | `style(course): 优化课程卡片间距` |
| `docs` | 文档 | `docs: 更新 API 文档` |
| `test` | 测试 | `test(course): 添加课程创建的单元测试` |
| `chore` | 杂项 | `chore: 更新依赖版本` |

```bash
# ✅ 好的提交
git commit -m "feat(enrollment): 添加课程报名表单验证

- 手机号格式校验（11位手机号）
- 课程是否存在校验
- 满员课程阻止报名
- 报名成功后发送通知"

# ❌ 差的提交
git commit -m "update"
git commit -m "fix bug"
git commit -m "。。。"
```

---

## 3. 代码审查机制

### 3.1 小团队 Code Review 流程

```
┌──────────┐     ┌──────────────┐     ┌──────────────┐     ┌──────────┐
│  开发完成  │ ──→ │  自查清单检查  │ ──→ │  提交 PR/MR  │ ──→ │  审查者   │
│  本地测试  │     │  (见 3.2)    │     │  描述变更内容  │     │  Review  │
└──────────┘     └──────────────┘     └──────────────┘     └────┬─────┘
                                                                │
                                                    ┌───────────┴───────────┐
                                                    ▼                       ▼
                                              ┌──────────┐          ┌──────────────┐
                                              │  通过 ✓   │          │ 需修改 → 反馈 │
                                              │  合并代码  │          │  打回修改     │
                                              └──────────┘          └──────────────┘
```

### 3.2 提交前自查清单

每次提交代码前，开发者必须确认以下事项：

```
□ 代码能正常运行，没有语法错误
□ 本地基础功能测试通过
□ 命名清晰，没有歧义缩写
□ 没有遗留的调试代码（dd()、console.log、var_dump）
□ 没有硬编码的配置（数据库密码、API Key 等）
□ 敏感信息已移至 .env 文件
□ 新增功能有对应的数据验证
□ 数据库查询没有 N+1 问题
□ Git 提交信息符合规范
□ 相关文档已更新（如有）
```

### 3.3 Code Review 检查清单

审查者对照以下清单进行 Review：

```
【功能正确性】
□ 代码是否实现了预期功能
□ 边界情况是否处理（空值、极值、并发）
□ 错误处理是否完善

【代码质量】
□ 是否符合编码规范（PSR-12 / 团队规范）
□ 命名是否清晰准确
□ 函数/方法是否过长（超过 50 行需评估）
□ 是否有重复代码可以提取
□ 是否遵循单一职责原则

【安全性】
□ 用户输入是否验证和过滤
□ SQL 是否使用参数绑定（防注入）
□ 是否有权限控制检查
□ 敏感数据是否加密存储

【性能】
□ 数据库查询是否合理（N+1、全表扫描）
□ 是否有不必要的循环查询
□ 缓存策略是否合理
□ 前端资源是否优化加载

【可维护性】
□ 逻辑是否清晰易懂
□ 复杂逻辑是否有注释说明
□ 是否硬编码了魔法数字
□ 后续修改是否容易
```

### 3.4 Code Review 沟通原则

```
┌────────────────────────────────────────────────────┐
│              Code Review 黄金法则                     │
├────────────────────────────────────────────────────┤
│                                                    │
│  1. 对事不对人 — 审查代码，不审查人                   │
│     ✅ "这段循环查询可能导致性能问题"                  │
│     ❌ "你怎么写的，这么低级的错误"                    │
│                                                    │
│  2. 给出解决方案 — 不只指出问题                       │
│     ✅ "建议用 with() 预加载关联，避免 N+1"           │
│     ❌ "这里有问题"                                  │
│                                                    │
│  3. 区分严重程度 — 不要眉毛胡子一把抓                  │
│     🔴 必须修改：安全漏洞、逻辑错误                   │
│     🟡 建议修改：性能优化、代码质量                   │
│     🟢 可选修改：风格偏好、命名建议                   │
│                                                    │
│  4. 鼓励好的实践 — 正反馈同样重要                     │
│     "这个查询作用域封装得很好，复用性很强 👍"          │
│                                                    │
│  5. 控制时间 — 单次 Review 不超过 30 分钟             │
│     超过说明 PR 太大，需要拆分                       │
│                                                    │
└────────────────────────────────────────────────────┘
```

---

## 4. 小团队开发流程

### 4.1 推荐开发流程（轻量级）

```
┌─────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌─────────┐
│ 需求理解  │ → │ 技术方案  │ → │ 编码实现  │ → │ 自测+Review │ → │  部署上线 │
│ 10%      │    │ 20%      │    │ 40%      │    │ 20%       │    │ 10%     │
└─────────┘    └──────────┘    └──────────┘    └──────────┘    └─────────┘
```

> **关键原则**：花在理解和设计上的时间，会在编码和调试阶段数倍返还。

### 4.2 需求理解阶段

```markdown
## 开工前必须搞清楚的 5 个问题

1. 这个功能要解决什么问题？（业务目标）
2. 用户是谁？使用场景是什么？（用户故事）
3. 有哪些输入和输出？（数据流）
4. 有哪些边界情况和异常场景？（边界条件）
5. 完成的标准是什么？（验收标准）

# 示例：课程报名功能
- 业务目标：让学生能在线报名课程
- 用户故事：作为学生，我填写报名表单，提交后收到确认通知
- 数据流：表单输入 → 验证 → 写入数据库 → 发送通知 → 显示成功
- 边界情况：课程满员、手机号重复、网络超时、并发报名
- 验收标准：
  □ 合法输入能成功报名
  □ 非法输入有明确错误提示
  □ 满员课程无法报名
  □ 报名成功后发送通知
```

### 4.3 技术方案设计（简化版）

对于小团队，不需要写冗长的设计文档，但需要回答：

```markdown
## 技术方案检查表

□ 涉及哪些数据表？需要新建表吗？
  → 需要新建 course_enrollments 表

□ 数据表结构设计：
  - id (主键)
  - course_id (外键)
  - student_name (string)
  - phone_number (string, unique)
  - status (enum: pending, confirmed, cancelled)
  - created_at, updated_at

□ 需要新建/修改哪些文件？
  → Migration、Model、Controller、Blade 视图、Form Request

□ 有没有可以复用的现有代码？
  → 可复用 Course 模型的 is_full 属性

□ 有没有性能风险？
  → 并发报名可能超卖，需要数据库事务 + 行锁
```

### 4.4 Git 分支策略（极简版）

```
main (生产环境)
 │
 ├── develop (开发环境)
 │    │
 │    ├── feature/course-enrollment (功能分支)
 │    │    └── 完成后合并回 develop
 │    │
 │    ├── fix/enrollment-validation (修复分支)
 │    │    └── 完成后合并回 develop
 │    │
 │    └── ...
 │
 └── hotfix/critical-bug (紧急修复)
      └── 直接合并回 main + develop
```

```bash
# 分支命名规范
feature/功能名     # 新功能    如：feature/course-enrollment
fix/问题名         # Bug修复   如：fix/login-redirect
hotfix/紧急修复    # 紧急修复  如：hotfix/payment-error
refactor/模块名    # 重构      如：refactor/course-service

# 工作流程
git checkout develop
git pull origin develop
git checkout -b feature/course-enrollment

# ... 开发 ...

git add .
git commit -m "feat(enrollment): 添加课程报名功能"

git push origin feature/course-enrollment
# → 创建 PR/MR → Code Review → 合并
```

---

## 5. 开发者能力成长路径

### 5.1 能力等级模型

```
Level 1 - 入门开发者（0-6个月）
├── 能独立完成简单的 CRUD 功能
├── 理解 MVC 基本概念
├── 能使用 Git 进行基本的版本管理
└── 能按照现有规范编写代码

Level 2 - 初级开发者（6个月-1.5年）
├── 能独立完成中等复杂度的功能模块
├── 理解数据库设计基本原则
├── 能编写基本的表单验证和数据校验
├── 理解并能应用设计模式（至少 3 种）
├── 能进行基本的性能优化（N+1 检测等）
└── 能参与 Code Review 并提出有效建议

Level 3 - 中级开发者（1.5-3年）
├── 能独立负责完整功能模块的设计和实现
├── 理解系统架构设计原则
├── 能编写单元测试和集成测试
├── 能处理复杂业务逻辑和边界情况
├── 理解安全防护最佳实践
├── 能指导 Level 1-2 的开发者
└── 能进行技术方案评审

Level 4 - 高级开发者（3年+）
├── 能负责系统架构设计和技术选型
├── 能处理高并发、大数据量场景
├── 理解 DevOps 和 CI/CD 流程
├── 能建立团队技术规范和流程
├── 能进行技术预研和风险评估
└── 能培养和提升团队能力
```

### 5.2 分阶段学习计划

#### Level 1 → Level 2 成长路径（重点突破）

```
┌────────────────────────────────────────────────────────┐
│                    前 3 个月                              │
├────────────────────┬───────────────────────────────────┤
│ PHP 基础强化        │ 类型系统、面向对象、命名空间、Composer│
│ Laravel 核心概念    │ 路由、控制器、模型、视图、中间件      │
│ 数据库基础          │ 表设计、迁移、Seeder、Eloquent ORM   │
│ Git 工作流          │ 分支管理、合并、冲突解决             │
└────────────────────┴───────────────────────────────────┤
│                    4-6 个月                              │
├────────────────────┬───────────────────────────────────┤
│ Livewire 交互开发   │ 组件化、状态管理、实时验证           │
│ 表单处理            │ Form Request 验证、文件上传         │
│ 认证授权            │ Laravel Breeze/Jetstream、权限控制  │
│ 调试技能            │ Laravel Telescope、日志、断点调试    │
└────────────────────┴───────────────────────────────────┤
│                    7-12 个月                             │
├────────────────────┬───────────────────────────────────┤
│ 设计模式实践        │ Repository、Service、Strategy       │
│ API 开发            │ RESTful API、API Resource、版本管理 │
│ 测试入门            │ PHPUnit、功能测试、Mock             │
│ 性能优化基础        │ N+1 检测、缓存、查询优化            │
└────────────────────┴───────────────────────────────────┘
```

#### 每日/每周练习建议

```markdown
## 每日（15-30分钟）
- 阅读一段优秀开源代码，理解其设计思路
- 做一道编程小练习（算法/设计模式）

## 每周（2-3小时）
- 完成一个完整的小功能模块（含测试）
- 写一篇技术笔记，总结本周学到的东西
- Review 一份同事的代码

## 每月
- 做一次项目代码回顾，找出可改进的地方
- 学习一个新的技术点并实践
- 分享一次技术心得（团队内 or 笔记）
```

### 5.3 推荐学习资源

| 资源 | 用途 | 优先级 |
|------|------|--------|
| [Laravel 官方文档](https://laravel.com/docs) | 权威参考，遇到问题先查文档 | ⭐⭐⭐⭐⭐ |
| [Livewire 官方文档](https://livewire.laravel.com) | Livewire 组件开发指南 | ⭐⭐⭐⭐⭐ |
| [FluxUI 官方文档](https://fluxui.dev) | UI 组件使用参考 | ⭐⭐⭐⭐⭐ |
| [Laracasts](https://laracasts.com) | 视频教程，实战性强 | ⭐⭐⭐⭐ |
| [PHP The Right Way](https://phptherightway.com) | PHP 最佳实践 | ⭐⭐⭐⭐ |
| [Refactoring Guru](https://refactoring.guru) | 设计模式（图文并茂） | ⭐⭐⭐⭐ |
| [Tailwind CSS 文档](https://tailwindcss.com/docs) | CSS 工具类参考 | ⭐⭐⭐⭐ |

---

## 6. 工具链配置指南

### 6.1 代码格式化与静态检查

#### Laravel Pint（PHP 代码格式化）

```bash
# 安装
composer require --dev laravel/pint

# 执行格式化
./vendor/bin/pint

# 检查（不修改，只报告）
./vendor/bin/pint --test
```

#### pint.json 配置

```json
{
    "preset": "laravel",
    "rules": {
        "array_syntax": {
            "syntax": "short"
        },
        "ordered_imports": {
            "sort_algorithm": "alpha"
        },
        "no_unused_imports": true,
        "not_operator_with_successor_space": true,
        "trailing_comma_in_multiline": true,
        "phpdoc_scalar": true,
        "unary_operator_spaces": true,
        "binary_operator_spaces": true,
        "blank_line_before_statement": {
            "statements": ["break", "continue", "declare", "return", "throw", "try"]
        },
        "phpdoc_separation": true,
        "phpdoc_align": true,
        "phpdoc_order": true
    }
}
```

#### Larastan（PHP 静态分析）

```bash
# 安装
composer require --dev larastan/larastan

# 分析
./vendor/bin/phpstan analyse
```

#### phpstan.neon 配置

```neon
includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: 6
    paths:
        - app/
    excludePaths:
        - app/Console/Kernel.php
    checkMissingIterableValueType: false
```

### 6.2 Git Hooks（提交前自动检查）

#### 安装 Husky（前端 Git Hooks）

```bash
npm install --save-dev husky lint-staged
npx husky install
npx husky add .husky/pre-commit "npx lint-staged"
```

#### .lintstagedrc.json

```json
{
    "*.php": [
        "./vendor/bin/pint --test"
    ],
    "*.blade.php": [
        "npx blade-formatter --write"
    ],
    "*.{js,vue}": [
        "npx prettier --write"
    ],
    "*.{css,scss}": [
        "npx prettier --write"
    ]
}
```

### 6.3 .editorconfig（统一编辑器配置）

```ini
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
indent_style = space
indent_size = 4
trim_trailing_whitespace = true

[*.md]
trim_trailing_whitespace = false

[*.{yml,yaml,json,js,vue,css,scss,blade.php}]
indent_size = 2

[*.blade.php]
indent_size = 4
```

### 6.4 VSCode 推荐配置与插件

#### 推荐插件清单

```json
{
    "recommendations": [
        "onecentlin.laravel-blade",          // Blade 语法高亮
        "onecentlin.laravel5-snippets",       // Laravel 代码片段
        "amiralizadeh.ember-cli",             // Livewire 代码片段
        "shufo.vscode-blade-formatter",       // Blade 格式化
        "bmewburn.vscode-intelephense-client", // PHP 智能提示
        "bradlc.vscode-tailwindcss",          // Tailwind 智能提示
        "esben.prettier-vscode",              // 前端格式化
        "ms-vscode-remote.remote-containers"   // Dev Container
    ]
}
```

#### .vscode/settings.json

```json
{
    "editor.formatOnSave": true,
    "editor.defaultFormatter": "esben.prettier-vscode",
    "[php]": {
        "editor.defaultFormatter": "bmewburn.vscode-intelephense-client"
    },
    "[blade]": {
        "editor.defaultFormatter": "shufo.vscode-blade-formatter"
    },
    "intelephense.environment.phpVersion": "8.3.0",
    "tailwindCSS.includeLanguages": {
        "blade": "html"
    },
    "files.associations": {
        "*.blade.php": "blade"
    }
}
```

---

## 7. 项目实战检查清单

### 7.1 新项目启动检查清单

```
项目初始化
□ Laravel 项目已创建
□ .env 文件已配置（数据库、缓存、邮件等）
□ APP_KEY 已生成
□ .env 已加入 .gitignore
□ 数据库迁移文件已创建
□ Git 仓库已初始化
□ .editorconfig 已配置
□ Laravel Pint 已安装并配置
□ Larastan 已安装并配置
□ Git Hooks 已配置

安全检查
□ 生产环境 APP_DEBUG=false
□ 数据库密码已修改（非默认）
□ CSRF 保护已启用（Laravel 默认）
□ XSS 防护已配置（Blade 默认转义）
□ 文件上传有类型和大小限制
□ 敏感操作有权限控制

性能基线
□ 数据库索引已规划
□ 配置缓存已了解（php artisan config:cache）
□ 路由缓存已了解（php artisan route:cache）
□ 视图缓存已了解（php artisan view:cache）
```

### 7.2 上线前检查清单

```
代码质量
□ 所有 TODO/FIXME 已处理或有 Issue 跟踪
□ 没有遗留的调试代码（dd/dump/console.log）
□ 没有硬编码的 URL/密码/API Key
□ Pint 检查通过（pint --test）
□ Larastan 分析通过
□ Code Review 已完成

功能验证
□ 所有功能在测试环境验证通过
□ 表单验证完整（前端 + 后端）
□ 错误页面已配置（404/500/503）
□ 空数据状态已处理
□ 加载状态已处理

数据库
□ 迁移文件可正常执行（migrate:fresh）
□ Seeder 可正常执行
□ 生产数据备份方案已确认

部署
□ 部署步骤已文档化
□ 回滚方案已确认
□ 环境变量已同步到生产
□ 定时任务已配置（cron）
□ 队列 worker 已配置（如使用队列）
```

---

## 附录：快速参考卡

### 常用 Artisan 命令

```bash
# 开发
php artisan serve                    # 启动开发服务器
php artisan tinker                   # 交互式 REPL
php artisan route:list               # 查看所有路由
php artisan db:seed                  # 填充测试数据

# 代码生成
php artisan make:model Course -mfsc  # 模型+迁移+工厂+Seeder+Controller
php artisan make:livewire CourseList # 创建 Livewire 组件
php artisan make:request StoreCourse # 创建 Form Request

# 数据库
php artisan migrate                  # 执行迁移
php artisan migrate:rollback         # 回滚上一次迁移
php artisan migrate:fresh --seed     # 重建数据库并填充

# 缓存
php artisan config:cache             # 缓存配置
php artisan route:cache              # 缓存路由
php artisan optimize                 # 优化所有缓存
php artisan optimize:clear           # 清除所有缓存

# 调试
php artisan telescope                # 启动 Telescope 调试面板
php artisan db:monitor               # 数据库监控
```

### 常见问题速查

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| 页面白屏 | PHP 错误 / 路由未定义 | 检查 `storage/logs/laravel.log` |
| N+1 查询 | 循环中访问关联模型 | 使用 `with()` 预加载 |
| 表单提交 419 | CSRF Token 缺失 | 确保表单包含 `@csrf` |
| 文件权限错误 | storage/bootstrap 缓存无权限 | `chmod -R 775 storage bootstrap/cache` |
| Composer 超时 | 国内网络问题 | 使用阿里云镜像 `composer config -g repo.packagist composer https://mirrors.aliyun.com/composer/` |

---

> **最后的话**：技术提升不是一蹴而就的事情，关键在于**持续练习 + 及时反馈 + 定期回顾**。建议团队每周安排 1 小时的技术分享会，每两周做一次代码回顾，保持学习和改进的节奏。
>
> 有具体的代码问题或技术选型需要讨论，随时找我。
>
> — 资深开发工程师（Senior Developer）
