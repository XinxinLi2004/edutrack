# Eduflow 代码审查标准与流程

> 版本：v1.0 ｜ 适用范围：前端（JS/CSS/HTML）、Python 后端、SQL、配置文件
> 适用团队：1–2 人小团队 ｜ 核心原则：**自动化优先，人工把关逻辑与架构**

---

## 一、总则与目标

### 1.1 为什么要有审查

你们目前代码质量参差不齐，具体痛点是：**可读性差、缺文档和规范、Bug 反复出现**，并且需要从**安全性、结构、可持续性、架构**四个更高维度做审查。

小团队人少，不可能搞重流程。所以本规范的设计哲学是：

- **机器挡在前面**：格式、风格、常见 Bug、安全漏洞，用工具在提交时和合并前自动拦住，不占用人的精力。
- **人只做机器做不了的事**：业务逻辑正确性、架构合理性、边界与异常处理、可读性主观判断。
- **小步提交**：每次改动聚焦一件事，审查才看得过来、看得懂。

### 1.2 目标（可度量）

| 目标 | 量化指标 | 期望 |
|------|----------|------|
| 风格统一 | 格式化/风格检查通过率 | 100%（提交即拦截） |
| 减少 Bug | 线上/测试逃逸 Bug 数 | 季度环比下降 |
| 提升可读性 | 代码评审“需重写”比例 | < 15% |
| 补齐文档 | 公共模块/接口文档覆盖率 | ≥ 90% |
| 安全可控 | 高危漏洞遗留数 | 0 |
| 可持续 | 单测覆盖率（核心模块） | ≥ 60% |

### 1.3 基本原则

1. **谁写谁负责**：作者对自己的代码质量负首要责任，审查者是第二道防线。
2. **审查是协作不是挑刺**：评论对事不对人，用建议语气（“建议……”/“此处是否……”）。
3. **阻塞性问题必须修**：安全漏洞、明显 Bug、破坏构建，必须修复才能合并；风格类可后续统一。
4. **小步快跑**：单 PR 改动控制在可一次审完（建议 < 400 行有效改动）。

---

## 二、审查分层架构

把审查拆成三层，越底层越自动化，越顶层越需要人脑：

```
┌─────────────────────────────────────────────────────────┐
│ 第 3 层：架构评审（定期，每月/每迭代）                    │
│   模块边界、技术债、扩展性、技术选型                       │
├─────────────────────────────────────────────────────────┤
│ 第 2 层：人工审查（PR 级，每次合并前）                    │
│   逻辑正确性、边界/异常、可读性、安全设计、结构合理性      │
├─────────────────────────────────────────────────────────┤
│ 第 1 层：自动静态审查（本地 pre-commit + CI，强制）       │
│   格式化、lint、类型检查、安全扫描、依赖漏洞、单测         │
└─────────────────────────────────────────────────────────┘
```

- **第 1 层是强制门禁**：不通过就提交不了 / 合并不了，零商量。
- **第 2 层是质量闸门**：1–2 人团队，作者提交后**自己作为 Reviewer 二次审视 diff**（换视角看自己写的代码），关键改动请对方互审。
- **第 3 层是方向校准**：防止项目长歪，技术债失控。

---

## 三、审查维度与标准（核心）

下面 8 个维度，每个都给出**标准、自动化工具、人工关注点**。前 4 个偏“不犯错”，后 4 个偏“长期健康”。

### 维度 1：代码风格（Style）—— 必须统一

| 检查项 | 标准 | 自动化工具 | 人工关注 |
|--------|------|-----------|----------|
| 格式化 | 统一用工具自动格式化，禁止手工对齐 | ruff format / Prettier | 无 |
| 命名 | 变量/函数小驼峰，类大驼峰，常量全大写下划线；见名知意 | ruff (N)、ESLint | 命名是否表意清晰 |
| 行宽/换行 | 行宽 ≤ 100（Python）/ ≤ 80–100（JS） | ruff / Prettier | — |
| 导入顺序 | 标准库→第三方→本地，分组 | ruff isort | — |
| 引号/分号 | JS 统一单引号、不省略分号（按团队约定） | Prettier | — |
| 无用代码 | 无未使用导入/变量/死代码 | ruff (F401/F841) / ESLint | 是否有“留着以后用”的注释代码（应删） |

### 维度 2：可读性（Readability）—— 让人看得懂

| 检查项 | 标准 | 自动化工具 | 人工关注 |
|--------|------|-----------|----------|
| 函数长度 | 单函数 ≤ 50 行，超过必须拆分 | ruff (C901 复杂度)、自定义规则 | 是否一个函数干了多件事 |
| 圈复杂度 | 单函数复杂度 ≤ 10 | ruff mccabe | 嵌套是否过深（>3 层需重构） |
| 重复代码 | 同一逻辑不重复 3 次以上 | ruff (duplicate)、JS 类似工具 | 是否可抽公共函数/工具 |
| 魔法值 | 禁止裸数字/字符串，提为具名常量 | ruff (magic-value) | 是否应定义为配置 |
| 注释 | 解释“为什么”而非“是什么”；不写废话注释 | — | 复杂逻辑是否有 why 注释 |
| 表达清晰 | 避免过巧的写法（嵌套三元、位运算炫技） | — | 半年后的自己能否一眼看懂 |

### 维度 3：文档与规范（Documentation）—— 必须补齐

| 检查项 | 标准 | 自动化工具 | 人工关注 |
|--------|------|-----------|----------|
| 模块文档 | 每个包/模块有 `__init__.docstring` 或 README 说明职责 | — | 新模块是否有职责说明 |
| 公共函数/类 | 公开 API 有 docstring（参数、返回、异常） | ruff (D) / 类型注解 | 签名是否自解释 |
| 接口文档 | HTTP 接口需标注方法/路径/入参/出参/错误码 | — | PR 模板是否填了接口说明 |
| 变更说明 | PR 必须填模板（做了啥、为啥、怎么测） | PR 模板必填校验 | 描述是否让人能审 |
| README | 项目/子服务有运行、配置、部署说明 | — | 新人能否照 README 跑起来 |

### 维度 4：Bug 防护（Defect Prevention）—— 别让低级错上线

| 检查项 | 标准 | 自动化工具 | 人工关注 |
|--------|------|-----------|----------|
| 空值/边界 | 对 None、空列表、空字符串、0 显式处理 | 类型检查(mypy)、lint | 是否假设了输入一定存在 |
| 异常处理 | 不裸 `except:`；捕获具体异常；不吞错 | ruff (E722/B) | 异常后资源是否释放、是否静默失败 |
| 资源释放 | 文件/连接/游标用 `with` 或 `try-finally` | ruff (S) | — |
| 并发/竞态 | 共享状态加锁或队列；不在请求间共享可变全局 | — | 是否有跨请求状态泄漏 |
| 类型注解 | 函数参数与返回尽量加类型注解 | mypy | 是否用 `Any` 掩盖问题 |
| 日志 | 关键分支/错误有日志，不打印敏感信息 | — | 线上能否靠日志定位问题 |

### 维度 5：安全性（Security）—— 零容忍

| 检查项 | 标准 | 自动化工具 | 人工关注 |
|--------|------|-----------|----------|
| 注入防护 | SQL 用参数化/ORM；不拼字符串执行 | bandit (B608)、SQLAlchemy | 任何字符串拼接 SQL |
| 敏感信息 | 密钥/密码不入库、不硬编码；用环境变量/密钥管理 | bandit、pre-commit detect-secrets | `.env` 是否误提交 |
| 认证授权 | 受保护接口必须鉴权；越权访问检查 | — | 是否信任了前端传的 userId |
| 输入校验 | 所有外部输入（请求体/参数/文件）做校验与清洗 | pydantic / 表单校验 | 是否直接用了未校验输入 |
| 依赖漏洞 | 第三方库无已知高危 CVE | pip-audit / npm audit / dependabot | — |
| XSS/CSRF | 前端转义输出；关键操作加 CSRF token | ESLint security 规则 | 是否 `v-html` 渲染未净化内容 |

### 维度 6：结构（Structure）—— 不堆屎山

| 检查项 | 标准 | 自动化工具 | 人工关注 |
|--------|------|-----------|----------|
| 模块边界 | 分层清晰（路由/服务/数据访问），不跨层调用 | — | 是否把 SQL 写进了视图层 |
| 单一职责 | 一个模块/类/函数只做一件事 | 复杂度工具 | 是否上帝类/万能函数 |
| 耦合度 | 模块间依赖明确、低耦合；避免循环依赖 | — | import 是否混乱 |
| 配置外置 | 环境相关（端口、密钥、开关）不放代码 | — | 是否有写死的 env 值 |
| 目录规范 | 遵循既定目录结构，不乱放文件 | — | 新文件是否放对位置 |

### 维度 7：可持续性（Sustainability）—— 能长期维护

| 检查项 | 标准 | 自动化工具 | 人工关注 |
|--------|------|-----------|----------|
| 测试 | 核心逻辑/工具函数有单测；改 Bug 先补复现测试 | pytest + coverage | 是否只改不测 |
| 可观测性 | 关键路径有日志/埋点；错误可追踪 | — | 出事能否定位 |
| 兼容性 | 接口变更向后兼容，或显式版本化 | — | 是否破坏了老调用方 |
| 技术债 | 临时方案加 `TODO(owner): 原因+计划` 注释 | — | 是否留了无期限的 TODO |
| 性能 | 无明显 N+1 查询、无全表扫描、无同步阻塞 | — | 循环里是否查了库 |

### 维度 8：架构（Architecture）—— 方向不出错（定期评审，非每次 PR）

| 检查项 | 标准 | 评审方式 | 人工关注 |
|--------|------|----------|----------|
| 分层架构 | 符合既定架构（如前后端分离、清晰分层） | 月度架构评审 | 是否引入了不当耦合 |
| 扩展性 | 新需求能否低成本接入，而非四处改 | 评审会议 | 是否需要抽象/插件化 |
| 技术选型 | 引入新框架/库需评审，避免碎片化 | 评审会议 | 是否重复造轮子/过度设计 |
| 数据模型 | 表结构/接口契约合理，演变可控 | 评审会议 | 是否建模混乱 |
| 边界合理性 | 服务/模块职责划分稳定 | 评审会议 | 是否频繁越界改动 |

---

## 四、审查流程（按步骤）

```
开发 → 自审(清单+pre-commit) → 提PR(填模板) → CI自动审查 → 人工审查(作者二次看/互审) → 修改 → 合并(squash) → 架构评审(定期)
```

### 步骤 1：开发前
- 任务明确、验收标准清晰（接口契约、边界条件先想清楚）。
- 从 `main` 拉特性分支：`feat/xxx`、`fix/xxx`、`refactor/xxx`。

### 步骤 2：开发中
- **小步提交**，每次 commit 聚焦一件事，message 用约定式（`feat:`/`fix:`/`refactor:`/`docs:`/`test:`/`chore:`）。
- 写代码同时补/改单测。

### 步骤 3：自审（提交前必做）
- 运行 `pre-commit`（自动格式化+lint+安全扫描）。
- 对照 `docs/REVIEW_CHECKLIST.md` 逐项自查。
- 自己 diff 一遍，假装是别人写的代码来看。

### 步骤 4：提 PR
- 用 `docs/PR_TEMPLATE.md` 填描述（做了啥/为啥/怎么测/影响范围）。
- 关联任务/需求编号。

### 步骤 5：CI 自动审查（门禁）
- CI 跑：格式化检查、lint、类型检查、安全扫描、依赖审计、单测+覆盖率。
- **任意一项失败 → 禁止合并**，作者修完重跑。

### 步骤 6：人工审查
- 1–2 人团队：**作者本人作为 Reviewer 再审一遍 diff**（隔一段时间或第二天看，效果更好），关键/高风险改动请对方互审。
- 审查者按 8 维度给意见，区分：
  - 🔴 **阻塞（Blocking）**：安全/明显 Bug/破坏构建，必须修。
  - 🟡 **建议（Suggestion）**：可读性/结构优化，建议修。
  - 🟢 **点赞（Nit）**：锦上添花或认可。
- 评论用建议语气，避免“你这里错了”。

### 步骤 7：修改与重审
- 作者逐条回应（修 or 说明为何不修）。
- 改完推新 commit，CI 重跑，审查者确认。

### 步骤 8：合并
- 用 **Squash Merge** 把特性分支压成单个整洁 commit，保持 `main` 历史干净。
- 删除特性分支。

### 步骤 9：架构评审（定期）
- 每月或每迭代一次，审视技术债、模块边界、新引入依赖、可持续性趋势。
- 输出：待办项 + 下次改进点，记到项目文档。

---

## 五、角色与职责

| 角色 | 职责 | 小团队怎么落地 |
|------|------|----------------|
| 开发者（作者） | 写代码、自审、补测试、修审查意见、填 PR | 李老师 / 搭档各自负责自己的 PR |
| 审查者（Reviewer） | 按 8 维度审逻辑/架构/安全，给分级意见 | 作者二次自审 + 关键改动互审 |
| 架构负责人 | 定规范、做架构评审、拍板技术选型 | 由资深一方担任，统一标准 |

---

## 六、推荐工具链

### Python（后端）
| 用途 | 工具 | 说明 |
|------|------|------|
| 格式化+lint 一体 | **ruff** | 快，替代 black+isort+flake8+pydocstyle |
| 类型检查 | **mypy** | 抓类型错误、None 处理 |
| 安全扫描 | **bandit** | 找硬编码密钥、危险函数调用 |
| 依赖漏洞 | **pip-audit** | 扫已知 CVE |
| 测试 | **pytest + pytest-cov** | 单测+覆盖率 |

### 前端（JS/CSS）
| 用途 | 工具 | 说明 |
|------|------|------|
| 格式化 | **Prettier** | JS/JSON/CSS/HTML 统一格式 |
| JS lint | **ESLint** | 规则见 `.eslintrc.json`，含安全相关 |
| CSS lint | **stylelint** | CSS 规范与潜在问题 |
| 依赖漏洞 | **npm audit** | 扫前端依赖 CVE |

### 通用
| 用途 | 工具 | 说明 |
|------|------|------|
| 提交门禁 | **pre-commit** | 本地提交前自动跑上述检查 |
| CI | **GitHub Actions / 你用的 CI** | PR 上强制跑全套 |
| 密钥防泄漏 | **detect-secrets** | 阻止密钥误提交 |
| 依赖更新 | **Dependabot / Renovate** | 自动升级、修漏洞 |

> 配置文件已全部生成在项目根目录：`pyproject.toml`、`.eslintrc.json`、`.prettierrc.json`、`.stylelintrc.json`、`.pre-commit-config.yaml`、`.github/workflows/code-review.yml`。

---

## 七、度量与持续改进

每月看一次这几个数，判断规范有没有用：

| 指标 | 怎么算 | 健康线 |
|------|--------|--------|
| 自动检查通过率 | 一次过 / 总提交 | ≥ 95% |
| 单测覆盖率 | coverage 报告 | 核心模块 ≥ 60% |
| Bug 逃逸率 | 上线后 Bug 数 / 总 Bug | 环比下降 |
| 高危漏洞遗留 | bandit/pip-audit 高危项 | 0 |
| 平均审查时长 | PR 开到合并的时长 | < 2 天 |
| 技术债趋势 | TODO 数 / 复杂度趋势 | 不恶化 |

**改进节奏**：每月架构评审时，回顾上表，挑 1–2 个最差项重点治理，别一次性铺太开。

---

## 八、1–2 人团队快速落地步骤

1. **装工具**（一次性）：`pip install pre-commit ruff mypy bandit pip-audit pytest pytest-cov`；前端 `npm i -D prettier eslint stylelint`。
2. **初始化 pre-commit**：`pre-commit install`（之后每次提交自动检查）。
3. **接 CI**：把 `.github/workflows/code-review.yml` 推上仓库，PR 强制跑。
4. **养成习惯**：每次提 PR 前先自审 checklist + 自己再审一遍 diff。
5. **每月一次架构评审**：对照第七节指标，挑重点治理。

> 规范不是一天养成的。先让“自动门禁”跑起来（零成本挡住大部分问题），再把人工审查的 8 维度 checklist 用熟，3 个月就能明显感觉到代码整齐了、Bug 少了。

---

## 附：文件清单

| 文件 | 作用 |
|------|------|
| `代码审查标准与流程.md` | 本文件，总标准与流程 |
| `docs/REVIEW_CHECKLIST.md` | 人工审查清单（按 8 维度，提交前/审查时用） |
| `docs/PR_TEMPLATE.md` | PR 描述模板 |
| `pyproject.toml` | Python 工具配置（ruff/mypy/bandit/pytest） |
| `.eslintrc.json` / `.prettierrc.json` / `.stylelintrc.json` | 前端工具配置 |
| `.pre-commit-config.yaml` | 本地提交门禁 |
| `.github/workflows/code-review.yml` | CI 自动审查流水线 |
