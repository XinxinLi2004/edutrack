# Eduflow 代码审查报告

> 审查对象：学员管理系统（Python 标准库 http.server + sqlite3 + 自研模板引擎）
> 审查日期：2026-07-12 ｜ 审查依据：`代码审查标准与流程.md` 的 8 维度
> 审查范围：`app.py`（~1500 行）、`database.py`（~1558 行）、`static/js/app.js`、`templates/*`（29 个）、`package.json`、`pyproject.toml`

---

## 一、总体结论

**结论：🟡 需修改后合并（有 4 项阻塞问题必须修复）**

代码整体**功能完整、可读性好、安全基础扎实**（SQL 全参数化、模板自动转义、CSV 带 BOM、前端用 `textContent` 防 XSS）。但存在 1 个**高危数据泄露漏洞**、1 个**可移植性硬伤**、1 个**数据一致性 Bug**，以及**零测试**和**单文件过大**的结构问题。

### 做得好的（应保留）
| 项 | 说明 |
|----|------|
| SQL 注入防护 | 全部查询用参数化 `?` 占位符，无字符串拼接（维度 5 ✅） |
| XSS 防护 | `VarNode.render` 对变量输出统一 `escape_html`；前端 `textContent`（维度 5 ✅） |
| 边界处理 | 大量 `_to_int/_to_float` 兜底、空值判断（维度 4 大部分 ✅） |
| 自动迁移 | `migrate_db` 用 `try/except` 容错 ALTER（维度 7 ✅） |
| 索引优化 | 关键查询列建了索引（维度 7 ✅） |
| 无 RCE | 全仓无 `eval()`/`exec()`，模板引擎自研表达式解析（维度 5 ✅） |

### 问题严重度汇总
| 严重度 | 数量 | 说明 |
|--------|------|------|
| 🔴 阻塞（必须修） | 4 | 路径穿越泄露数据库/源码、package.json 硬编码路径、毕业届计算不一致、零鉴权隐患 |
| 🟡 应修复 | 8 | 单文件过大、无测试、int 转换无保护、构建产物/PII 入库、绑定 0.0.0.0 等 |
| 🟢 建议 | 5 | 无用导入、分页 `&` 未转义、内存全量加载等 |

---

## 二、按 8 维度逐项审查

### 维度 1：代码风格 — ✅ 良好
- ruff 配置已就绪（`pyproject.toml`），但**未实际执行过**（无 pre-commit/CI，见维度 7）。
- 命名、缩进、行宽基本统一。小问题：`database.py` 第 12 行 `import random` 未使用。
- 🟢 建议：在合并前跑一次 `ruff check .` 和 `ruff format --check .`，把存量问题批量清理。

### 维度 2：可读性 — ✅ 良好 / 🟡 结构
- 函数级可读性不错，注释到位（如 CSV 列名映射、迁移逻辑）。
- 🟡 问题：`app.py` 单类 `AppHandler` 承载约 60 个路由方法（~1500 行），`database.py` 单文件 ~1558 行。虽已做 HTTP 与数据访问分离，但单文件过大，违反单一职责（维度 6）。

### 维度 3：文档与规范 — 🟡 部分缺失
- 有 `README.md`/`PROJECT.md`/`架构设计.md`/`使用说明.md`，整体文档较全。
- 🟢 建议：公共函数已有 docstring；但 `app.py` 内多数路由方法缺 docstring（ruff `D` 规则会报）。API 层（`/api/*`）无接口文档。

### 维度 4：Bug 防护 — 🟡 有漏洞
| 项 | 位置 | 问题 |
|----|------|------|
| 用户输入 int 转换无保护 | `app.py` 多处：`int(query.get('page',['1'])[0])`、`int(class_id)`、`int(student_id)`(query) | 非数字（如 `?page=abc`）→ 未捕获 `ValueError` → 500 |
| attendance_save 键解析 | `app.py:876` `int(key.replace('status_',''))` | 若前端产生 `status_` 空键 → 500 |
| 删除未校验存在性 | 各 `delete_*` | 无效 id 静默 0 行（影响小，但无反馈） |

> 注：SQL 查询本身参数化，无注入类 Bug。

### 维度 5：安全性 — 🔴 有高危漏洞
| 编号 | 位置 | 问题 | 严重度 |
|------|------|------|--------|
| S1 | `app.py:468-495` `serve_static` | **路径穿越**：`file_path = os.path.join(ASSETS_DIR, path.lstrip('/'))` 后未做 containment 校验。`GET /static/../data/database.sqlite` 可下载**整个学员数据库（含姓名/手机/家长电话/地址）**；`/static/../app.py` 可下载源码。 | 🔴🔴 |
| S2 | `app.py:1473` `run_server` | 绑定 `0.0.0.0`，在内网任意机器可访问；与 S1 叠加风险放大。本地离线应用建议绑 `127.0.0.1`。 | 🟡 |
| S3 | 全路由 | **零鉴权**：`users` 表有 `password_hash`，但无任何登录/会话逻辑，所有路由（删除、批量导入、缴费）完全开放。当前本地单用户可容忍，一旦联网必须加鉴权。 | 🔴（联网场景）/🟡（纯本地） |
| S4 | `package.json:7-8` | **硬编码开发者机器绝对路径** `/Users/seikaya/.workbuddy/binaries/python/versions/3.13.12/bin/python3`。分发后 `npm start` 在别的机器直接失败，且泄露用户主目录。 | 🔴（可移植性硬伤） |
| S5 | `data/database.sqlite` 入库 | 含学员 PII 的数据库文件在仓库内（`data/`、`dist/` 均有 `.sqlite`），违反"敏感数据不外提"。 | 🟡 |

### 维度 6：结构 — 🟡 单文件过大
- 路由与数据访问已分层（`app.py` ↔ `database.py`），方向正确。
- 🟡 `app.py` / `database.py` 单文件过长，建议按业务域拆分模块（students/classes/courses/grades/assignments/payments），`app.py` 拆为各域 handler 或 blueprint。
- 🟡 CRUD 样板高度重复（每个实体 create/update/delete 同构），可抽通用 helper 降低出错面。
- 🟢 `get_db()` 每请求新建连接且未开 WAL，高并发写可能锁表；建议连接池或 `PRAGMA journal_mode=WAL`。

### 维度 7：可持续性 — 🔴 零测试 / 🟡 缺工程化
- 🔴 **零测试文件**，覆盖率为 0，远低于标准（核心模块 ≥60%）。`database.py` 的纯函数（`compute_graduation_year`、`_to_int/_to_float`、CSV 解析、import 逻辑）最易回归，应优先补单测。
- 🟡 **无 .gitignore**：`build/`、`dist/`、`student-system-windows.zip`、多个 `*.sqlite` 构建产物/数据库入库，应排除。
- 🟡 `pyproject.toml` 的 coverage `source = ["app","src"]` 与实际文件（`app.py`/`database.py` 在根目录）不符；无 `tests/` 目录。
- 🟢 无 CI（`.github/workflows/code-review.yml` 已提供但未接入仓库）。

### 维度 8：架构 — ✅ 合理 / 🟡 一处数据 Bug
- 分层、模块边界清晰，技术选型（零依赖标准库）契合"本地离线"定位，架构合理。
- 🔴 **毕业届次计算不一致（数据 Bug）**：`compute_graduation_year()`（database.py:88）用 `cohort + 3`（高中三年，正确，且 `MEMORY.md` 已约定）；但 `migrate_db()`（database.py:66）用 `graduation = (cohort + 2) % 100`。**老数据毕业届算错一年，且与新数据不一致**。应统一调用 `compute_graduation_year`。

---

## 三、阻塞问题详解与修复建议

### 🔴 S1：serve_static 路径穿越（最高优先）
**现状**（app.py:468-495）：
```python
file_path = os.path.join(ASSETS_DIR, path.lstrip('/'))
if not os.path.exists(file_path) or not os.path.isfile(file_path):
    self.send_error(404); return
```
`path` 来自 URL，未归一化。`/static/../data/database.sqlite` → 解析到 `ASSETS_DIR/../data/database.sqlite` → 越出 static 目录，下载数据库。

**修复**：join 后用 `os.path.realpath` 限制在 `STATIC_DIR` 内：
```python
file_path = os.path.realpath(os.path.join(STATIC_DIR, path[len('/static/'):]))
if not file_path.startswith(os.path.realpath(STATIC_DIR) + os.sep) or not os.path.isfile(file_path):
    self.send_error(404); return
```
并建议把 `serve_static` 的 base 从 `ASSETS_DIR` 改为专门的 `STATIC_DIR`，杜绝读取源码/数据库。

### 🔴 S4：package.json 硬编码路径
**修复**：改为
```json
"scripts": {
  "start": "python3 app.py",
  "seed": "python3 seed.py"
}
```
并在 `README` 说明依赖 Python 3.10+。

### 🔴 毕业届计算不一致
**修复**：database.py:66 改为 `graduation = compute_graduation_year(cohort)`，删除本地 `+2` 计算。已存在的错误数据需一次性校正脚本（或接受历史偏差，仅保证新数据正确）。

### 🔴 S3：鉴权
本地单用户可暂不改，但需在 `README` 明确"当前无鉴权，禁止在公网部署"；若计划联网，必须在 `AppHandler` 入口加登录态校验（users 表已具备密码字段）。

---

## 四、优先级修复清单（建议顺序）

| 序 | 项 | 严重度 | 工作量 | 说明 |
|----|----|--------|--------|------|
| 1 | 修复 S1 路径穿越 + S2 绑 127.0.0.1 | 🔴 | 0.5h | 防数据泄露，最高优先 |
| 2 | package.json 去硬编码路径 | 🔴 | 10min | 否则分发即废 |
| 3 | 统一毕业届计算为 +3 | 🔴 | 10min | 改 1 行 + 校正脚本 |
| 4 | 加 `.gitignore`，移除仓库内 `.sqlite` | 🟡 | 20min | 防 PII/产物入库 |
| 5 | 给 query 参数 int 转换加保护 | 🟡 | 1h | 防 500 |
| 6 | database.py 纯函数补单测（先达 30%） | 🟡 | 0.5d | 防回归，逐步提覆盖 |
| 7 | 跑 ruff 清理存量 + 接 pre-commit/CI | 🟡 | 1h | 把标准落地 |
| 8 | app.py/database.py 按域拆分 | 🟢 | 2d+ | 长期结构优化，非紧急 |

---

## 五、复用既有审查资产

本仓库已具备：`代码审查标准与流程.md`、`docs/REVIEW_CHECKLIST.md`、`docs/PR_TEMPLATE.md`、`pyproject.toml`（ruff/mypy/bandit/pytest 配置）、`.pre-commit-config.yaml`、`.github/workflows/code-review.yml`。

**下一步建议**：先修 1–4 项阻塞问题 → 接 pre-commit + CI（门禁自动跑 ruff/bandit）→ 用 `REVIEW_CHECKLIST.md` 对本次修改做自审 → 补 `database.py` 单测。

> 本报告的"🔴 阻塞项"已纳入 `docs/REVIEW_CHECKLIST.md` 的对应维度，后续每次改动请逐条核对。
