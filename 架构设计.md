# 学员管理系统架构设计

> 项目：学员管理系统（教培行业版）
> 技术栈：Node.js + Express + SQLite + Tailwind CSS + Alpine.js
> 部署方式：本地运行、离线可用
> 作者：资深开发工程师（Senior Developer）

---

## 1. 项目背景

- **使用方**：高中物理教培从业者
- **用户规模**：100-300 人
- **部署环境**：本地 Mac，要求离线可用
- **核心目标**：管理学员、班级、课程、成绩、出勤、作业、缴费，提供数据看板

---

## 2. 技术选型

| 层级 | 技术 | 选择理由 |
|------|------|---------|
| 运行时 | Node.js 22.x | 已预装在当前环境，无需额外安装 |
| 后端框架 | Express 4.x | 轻量、成熟、文档丰富，适合中小项目 |
| 数据库 | SQLite 3 | 单文件数据库，本地离线运行，无需配置服务器 |
| ORM | Better-sqlite3 | 同步 API，性能优秀，适合本地应用 |
| 前端样式 | Tailwind CSS 3.x | 实用类优先，快速构建现代 UI |
| 前端交互 | Alpine.js 3.x | 轻量级响应式框架，无需构建步骤 |
| 模板引擎 | EJS | 简单直观，与 Express 集成良好 |
| 图标 | Lucide Icons | 简洁现代，轻量 |

---

## 3. 功能模块

```
学员管理系统
├── 仪表盘 Dashboard
│   ├── 学员总数、班级数、课程数
│   ├── 本周待上课程
│   ├── 缴费统计
│   └── 最近动态
├── 学员管理
│   ├── 学员列表（搜索、筛选、分页）
│   ├── 新增学员
│   ├── 编辑学员
│   ├── 学员详情
│   └── 导出学员信息
├── 班级管理
│   ├── 班级列表
│   ├── 新增/编辑班级
│   ├── 班级学员
│   └── 班级课程安排
├── 课程管理
│   ├── 课程列表
│   ├── 新增/编辑课程
│   ├── 课程日历
│   └── 课程签到
├── 成绩管理
│   ├── 成绩录入
│   ├── 成绩列表
│   ├── 成绩分析（平均分、排名变化）
│   └── 成绩趋势图
├── 作业管理
│   ├── 作业布置
│   ├── 作业提交状态
│   └── 作业批改
├── 出勤管理
│   ├── 出勤记录
│   ├── 出勤统计
│   └── 缺勤提醒
└── 缴费管理
    ├── 缴费记录
    ├── 欠费提醒
    └── 缴费统计
```

---

## 4. 数据库设计

### 4.1 实体关系图

```
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│   students  │       │   classes   │       │   courses   │
│  （学员）    │       │  （班级）    │       │  （课程）    │
└──────┬──────┘       └──────┬──────┘       └──────┬──────┘
       │                     │                     │
       │ N:1                 │ 1:N                 │ 1:N
       │                     │                     │
       │            ┌────────┴────────┐           │
       │            │  class_students   │           │
       │            │  （班级学员关系）  │           │
       │            └────────┬────────┘           │
       │                     │                     │
       │                     │                     │
       │            ┌────────┴────────┐            │
       │            │ course_attendances│          │
       │            │   （课程出勤）     │          │
       │            └─────────────────┘           │
       │                                          │
       │ N:1                                      │
       │                                          │
┌──────┴──────┐                          ┌────────┴──────┐
│    grades   │                          │  assignments    │
│  （成绩）    │                          │  （作业）       │
└─────────────┘                          └─────────────────┘
       │
       │ N:1
       │
┌──────┴──────┐
│  payments   │
│  （缴费）    │
└─────────────┘
```

### 4.2 数据表结构

#### students（学员表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PRIMARY KEY | 自增 ID |
| name | TEXT NOT NULL | 姓名 |
| phone | TEXT | 手机号 |
| wechat | TEXT | 微信号 |
| school | TEXT | 学校 |
| grade | TEXT | 年级（高一/高二/高三） |
| parent_name | TEXT | 家长姓名 |
| parent_phone | TEXT | 家长电话 |
| address | TEXT | 地址 |
| status | TEXT | active/inactive |
| note | TEXT | 备注 |
| created_at | TEXT | 创建时间 |
| updated_at | TEXT | 更新时间 |

#### classes（班级表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PRIMARY KEY | 自增 ID |
| name | TEXT NOT NULL | 班级名称 |
| grade | TEXT | 目标年级 |
| subject | TEXT | 科目 |
| max_students | INTEGER | 最大人数 |
| teacher_name | TEXT | 教师姓名 |
| status | TEXT | active/inactive |
| description | TEXT | 描述 |
| created_at | TEXT | 创建时间 |

#### class_students（班级学员关系表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PRIMARY KEY | 自增 ID |
| class_id | INTEGER | 班级 ID |
| student_id | INTEGER | 学员 ID |
| join_date | TEXT | 加入日期 |
| status | TEXT | active/inactive |

#### courses（课程表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PRIMARY KEY | 自增 ID |
| class_id | INTEGER | 班级 ID |
| title | TEXT | 课程标题 |
| scheduled_at | TEXT | 上课时间 |
| duration | INTEGER | 时长（分钟） |
| location | TEXT | 上课地点 |
| status | TEXT | planned/completed/cancelled |
| description | TEXT | 课程描述 |
| created_at | TEXT | 创建时间 |

#### course_attendances（课程出勤表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PRIMARY KEY | 自增 ID |
| course_id | INTEGER | 课程 ID |
| student_id | INTEGER | 学员 ID |
| status | TEXT | present/absent/late/leave |
| note | TEXT | 备注 |
| created_at | TEXT | 创建时间 |

#### grades（成绩表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PRIMARY KEY | 自增 ID |
| student_id | INTEGER | 学员 ID |
| class_id | INTEGER | 班级 ID |
| exam_name | TEXT | 考试名称 |
| subject | TEXT | 科目 |
| score | REAL | 分数 |
| max_score | REAL | 满分 |
| exam_date | TEXT | 考试日期 |
| note | TEXT | 备注 |
| created_at | TEXT | 创建时间 |

#### assignments（作业表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PRIMARY KEY | 自增 ID |
| class_id | INTEGER | 班级 ID |
| title | TEXT | 作业标题 |
| content | TEXT | 作业内容 |
| deadline | TEXT | 截止日期 |
| status | TEXT | active/completed |
| created_at | TEXT | 创建时间 |

#### assignment_submissions（作业提交表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PRIMARY KEY | 自增 ID |
| assignment_id | INTEGER | 作业 ID |
| student_id | INTEGER | 学员 ID |
| status | TEXT | submitted/missing/late |
| score | REAL | 得分 |
| submitted_at | TEXT | 提交时间 |
| note | TEXT | 备注 |

#### payments（缴费表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PRIMARY KEY | 自增 ID |
| student_id | INTEGER | 学员 ID |
| class_id | INTEGER | 班级 ID |
| amount | REAL | 金额 |
| type | TEXT | tuition/material/other |
| status | TEXT | paid/unpaid/overdue |
| paid_at | TEXT | 缴费日期 |
| due_date | TEXT | 截止日期 |
| note | TEXT | 备注 |
| created_at | TEXT | 创建时间 |

#### users（系统用户表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PRIMARY KEY | 自增 ID |
| username | TEXT | 用户名 |
| password_hash | TEXT | 密码哈希 |
| role | TEXT | admin/teacher |
| created_at | TEXT | 创建时间 |

---

## 5. 页面结构

| 路由 | 页面 | 说明 |
|------|------|------|
| GET / | 仪表盘 | 系统首页、数据看板 |
| GET /students | 学员列表 | 搜索、筛选、分页 |
| GET /students/new | 新增学员 | 表单页面 |
| GET /students/:id | 学员详情 | 基本信息、成绩、出勤、缴费 |
| GET /students/:id/edit | 编辑学员 | 表单页面 |
| GET /classes | 班级列表 | 班级管理 |
| GET /classes/:id | 班级详情 | 学员、课程、成绩 |
| GET /courses | 课程列表 | 课程管理 |
| GET /courses/:id | 课程详情 | 出勤签到 |
| GET /grades | 成绩列表 | 成绩录入与分析 |
| GET /assignments | 作业列表 | 作业管理 |
| GET /attendances | 出勤记录 | 出勤统计 |
| GET /payments | 缴费记录 | 缴费管理 |
| GET /settings | 系统设置 | 用户管理等 |

---

## 6. 项目目录结构

```
student-management-system/
├── app.js                 # Express 主入口
├── database.js            # SQLite 数据库连接和初始化
├── package.json           # 依赖配置
├── seed.js                # 测试数据生成
├── public/                # 静态资源
│   ├── css/
│   │   └── style.css      # Tailwind 构建后的样式
│   ├── js/
│   │   └── app.js         # 前端交互脚本
│   └── images/
├── routes/                # 路由处理
│   ├── index.js           # 仪表盘路由
│   ├── students.js
│   ├── classes.js
│   ├── courses.js
│   ├── grades.js
│   ├── assignments.js
│   ├── attendances.js
│   ├── payments.js
│   └── api.js             # API 接口（JSON 数据）
├── views/                 # EJS 模板
│   ├── partials/
│   │   ├── header.ejs
│   │   ├── sidebar.ejs
│   │   └── footer.ejs
│   ├── index.ejs          # 仪表盘
│   ├── students/
│   │   ├── index.ejs
│   │   ├── new.ejs
│   │   ├── edit.ejs
│   │   └── show.ejs
│   ├── classes/
│   ├── courses/
│   ├── grades/
│   ├── assignments/
│   ├── attendances/
│   └── payments/
├── data/                  # 数据目录
│   └── database.sqlite    # SQLite 数据库文件
└── tailwind.config.js     # Tailwind 配置
```

---

## 7. 非功能需求

- **性能**：页面响应 < 1.5 秒，本地 SQLite 查询足够支持 300 人规模
- **安全**：密码使用 bcrypt 哈希，SQL 使用参数化查询，XSS 使用模板转义
- **离线**：数据库本地存储，无需网络
- **备份**：数据库为单文件，可一键复制备份
- **响应式**：适配桌面和 Pad 访问
- **主题**：支持浅色/深色/跟随系统主题切换

---

## 8. 开发计划

| 阶段 | 任务 | 时间估算 |
|------|------|---------|
| 1 | 项目初始化、数据库初始化 | 15% |
| 2 | 学员/班级/课程核心模块 | 30% |
| 3 | 成绩/作业/出勤/缴费模块 | 30% |
| 4 | 仪表盘、主题系统、高级 UI | 15% |
| 5 | 测试数据、功能验证、部署文档 | 10% |

---

> 备注：本架构设计针对当前环境（无 PHP/无 Homebrew）采用 Node.js 方案，确保能立即落地并本地运行。如果未来 PHP 环境可用，可平滑迁移到 Laravel + Livewire + FluxUI 方案。
