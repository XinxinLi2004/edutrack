# 理学优课学员管理系统 — 使用说明

> 版本 v2.0 | 最后更新：2026-07-08

---

## 目录

1. [系统简介](#1-系统简介)
2. [安装与启动](#2-安装与启动)
3. [仪表盘](#3-仪表盘)
4. [学员管理](#4-学员管理)
5. [班级管理](#5-班级管理)
6. [课程管理](#6-课程管理)
7. [成绩管理](#7-成绩管理)
8. [作业管理](#8-作业管理)
9. [出勤记录](#9-出勤记录)
10. [缴费管理](#10-缴费管理)
11. [学习追踪](#11-学习追踪)
12. [数据备份与迁移](#12-数据备份与迁移)
13. [常见问题](#13-常见问题)
14. [跨平台部署（Windows/Linux）](#14-跨平台部署windowslinux)

---

## 1. 系统简介

理学优课学员管理系统是一个**本地运行、离线可用**的教培管理工具，帮助您管理：

- 🧑‍🎓 学员档案与分班
- 📚 班级与课程安排
- 📊 考试成绩与作业跟踪
- 📝 出勤签到与缴费记录
- 📈 学习趋势分析

### 技术特点

| 特性 | 说明 |
|------|------|
| 离线运行 | 无需网络，数据存在本地 |
| 零安装 | Python 环境下双击即用；打包版无需 Python |
| 数据安全 | SQLite 单文件数据库，复制即备份 |
| 轻量高效 | 占用内存 < 50MB，页面响应 < 10ms |

### 界面总览

系统采用侧边栏导航 + 顶栏操作区的布局。左侧菜单包含 8 个功能模块，点击即可切换。

```
┌──────────────┬──────────────────────────────────┐
│  理学优课     │  [页面标题]          [+ 新增按钮] │
│  ─────────   │ ──────────────────────────────── │
│  📊 仪表盘   │                                   │
│  👨‍🎓 学员管理 │       页面主体内容区域             │
│  📚 班级管理 │                                   │
│  📅 课程管理 │                                   │
│  📈 成绩管理 │                                   │
│  📝 作业管理 │                                   │
│  ✔️ 出勤记录 │                                   │
│  💰 缴费管理 │                                   │
│              │                                   │
│  ☀️ 🌙 🖥️   │                                   │
└──────────────┴──────────────────────────────────┘
```

右上角三个按钮切换界面主题：☀️ 浅色 / 🌙 深色 / 🖥️ 跟随系统。

---

## 2. 安装与启动

### 方式一：打包版（推荐，无需安装 Python）

从 `dist/` 目录获取 `理学优课学员管理系统`，双击运行即可。

macOS 首次运行可能提示"无法验证开发者"：

```bash
# 方法1：右键 → 打开 → 确认打开
# 方法2：终端执行
xattr -dr com.apple.quarantine "理学优课学员管理系统"
```

启动后控制台会显示：

```
🚀 学员管理系统已启动
📍 访问地址: http://localhost:3000
💾 数据库位置: .../dist/data/database.sqlite
```

浏览器打开 `http://localhost:3000` 即可使用。

> **注意**：打包版不会有桌面窗口弹出，它是一个后台服务。使用完毕后直接关闭终端窗口或按 Ctrl+C 停止。

### 方式二：源码运行（需 Python 3.9+）

```bash
# 1. 安装 Python（前往 python.org 下载）
# 2. 双击启动（macOS）
python3 app.py

# 或命令行启动
cd 项目目录
python3 app.py

# Windows
python app.py
```

首次使用前，可先生成示例数据体验：

```bash
python3 seed.py
```

---

## 3. 仪表盘

**访问**：首页 `/`

仪表盘提供全局数据概览：

| 指标卡片 | 内容 |
|----------|------|
| 在读学员 | 状态为"在读"的学员总数 |
| 活跃班级 | 状态为"正常"的班级数 |
| 本月课程 | 当月安排的课程数 |
| 欠费学员 | 有未缴/逾期费用的学员数 |

下方展示**近期课程提醒**，列出未来一周内的课程安排。

---

## 4. 学员管理

**访问**：`/students`

### 4.1 查看学员列表

列表展示所有学员，每页 15 条。支持的筛选条件：

| 筛选 | 说明 |
|------|------|
| 搜索框 | 按姓名/手机号/学校模糊搜索 |
| 级次 | 按入学年份筛选（如 26级） |
| 年级 | 高一 / 高二 / 高三 |
| 状态 | 在读 / 已退学 |

点击学员行可进入详情页。

### 4.2 新增学员

点击右上角 **「➕ 新增学员」**，填写表单：

| 字段 | 必填 | 说明 |
|------|------|------|
| 姓名 | ✅ | 学员真实姓名 |
| 联系电话 | | 手机号 |
| 微信 | | 微信号 |
| 级次 | ✅ | 入学年份，如 26（自动计算毕业届次为 29届） |
| 当前年级 | | 高一/高二/高三 |
| 学校 | | 就读学校 |
| 家长姓名/电话 | | 家长联系方式 |
| 状态 | | 在读（默认）/ 已退学 |

### 4.3 编辑与删除

每个学员行右侧有 **✏️ 编辑** 和 **🗑️ 删除** 按钮。删除前会弹出确认框。

### 4.4 批量导入

点击 **「批量导入」**，支持 CSV 格式：

```csv
姓名,级,年级,学校,手机
张三,26,高一,合肥一中,13800138001
李四,25,高三,合肥六中,13800138002
```

导入前会显示**数据预览**，确认无误后点击导入。

### 4.5 批量操作

点击 **「批量操作」** 进入批量操作页：

1. 先用筛选条件缩小范围（搜索/级次/年级/状态）
2. 勾选要操作的学员（可点表头全选）
3. 选择操作类型：
   - **批量加入班级**：选择目标班级，所有勾选学员加入
   - **批量修改信息**：统一修改年级/级次/状态/学校（留空 = 不修改）
4. 点击「确认批量操作」

---

## 5. 班级管理

**访问**：`/classes`

### 5.1 创建班级

点击 **「新增班级」**，填写：

| 字段 | 说明 |
|------|------|
| 班级名称 | 如"新高一物理理优班" |
| 级次 | 入学年份（26） |
| 年级 | 高一/高二/高三 |
| 科目 | 物理/数学/化学等 |
| 授课教师 | 教师姓名 |
| 人数上限 | 班级容量 |

### 5.2 班级详情

进入班级详情页后，可以：

- **查看班级信息**：名称、级次、人数等
- **管理学员**：
  - 输入框搜索学员姓名，点击添加
  - 每位学员右侧有「移出」按钮
- **查看课程安排**：该班级所有课程的列表，可直接「新增课程」

### 5.3 添加学员搜索

在班级详情页底部，直接在输入框内**输入学员姓名**即可自动匹配。不再需要从下拉列表翻找。

---

## 6. 课程管理

**访问**：`/courses`

### 6.1 新增课程

点击 **「新增课程」**，填写：

| 字段 | 必填 | 说明 |
|------|------|------|
| 课程标题 | ✅ | 如"运动学基础" |
| 所属班级 | ✅ | 关联的班级 |
| 上课方式 | | **线下** / **线上** / **线上线下同步** |
| 上课时间 | | 日期+时间 |
| 时长 | | 分钟（默认 120） |
| 地点/链接 | | 教室名或腾讯会议链接 |
| 状态 | | 计划中 / 已完成 / 已取消 |

### 6.2 出勤签到

进入课程详情页，页面下方显示**出勤签到表**：

每个学员可以选择：

| 出勤状态 | 说明 |
|----------|------|
| 出勤 | 正常到场 |
| 线上参与 | 通过线上方式参加 |
| 缺勤 | 未到 |
| 迟到 | 迟到但到场 |
| 请假 | 请假未到场 |

| 参与方式 | 说明 |
|----------|------|
| 线下 | 教室上课 |
| 线上 | 远程上课 |

修改后点击 **「保存出勤」** 按钮。

---

## 7. 成绩管理

**访问**：`/grades`

### 7.1 查看成绩

成绩列表**按班级分组**展示，每个班级一个区块。

右侧统计面板显示：

| 指标 | 说明 |
|------|------|
| 平均分 | 当前筛选条件下的平均分 |
| 最高分 | 最高得分 |
| 最低分 | 最低得分 |
| 参考人数 | 有成绩记录的学员数 |

下方**成绩分布图**按分数段展示：90-100 / 80-89 / 70-79 / 60-69 / 60以下。

### 7.2 新增成绩

点击 **「新增成绩」**，填写：学员、考试名称、科目、得分、满分、考试日期。

### 7.3 导入成绩（CSV）

点击 **「导入成绩」**，粘贴 CSV 内容：

```csv
姓名,班级,考试名称,科目,得分
张三,新高一物理理优班,期中考试,物理,95
李四,新高一物理理优班,期中考试,物理,88
```

支持的表头（自动识别）：姓名/学员姓名、班级、考试名称/考试、科目、得分/分数/成绩、满分、考试日期、备注。

### 7.4 导出成绩

点击 **「导出成绩」** 下载 CSV 文件，可用 Excel 直接打开。

---

## 8. 作业管理

**访问**：`/assignments`

### 8.1 布置作业

点击 **「新增作业」**，填写：标题、班级、截止日期、作业内容。

### 8.2 查看提交情况

作业列表显示**提交进度**（已提交/总人数）和**平均分**。支持按班级筛选和按平均分排序。

### 8.3 修改提交状态与成绩

进入作业详情页，可直接在表格中修改：

| 可修改 | 操作方式 |
|--------|---------|
| 提交状态 | 下拉选择：已提交 / 迟交 / 未交 |
| 得分 | 输入数字 |
| 备注 | 输入文字 |

修改后点击 **「保存提交状态」**。

### 8.4 批量导入作业成绩

点击 **「导入成绩」**，粘贴 CSV：

```csv
姓名,得分
张三,95
李四,88
```

导入规则：
- CSV 中的学员 → 状态设为「已提交」+ 填入成绩
- CSV 中没有的学员（同班级）→ 自动标「未提交」

---

## 9. 出勤记录

**访问**：`/attendances`

按班级筛选查看课程出勤记录。出勤记录由课程详情页的签到表自动生成。

---

## 10. 缴费管理

**访问**：`/payments`

### 新增缴费

填写：学员、班级、金额、类型（学费/教材/其他）、缴费状态（已缴/未缴/逾期）、缴费日期、截止日期。

列表支持按学员和缴费状态筛选，逾期缴费会红色标记。

---

## 11. 学习追踪

**访问**：学员详情页 → 点击 **「📈 学习追踪」**

展示该学员所有作业成绩的**折线趋势图**：

- 蓝色实线：各次作业成绩
- 黄色虚线：个人平均分
- 下方表格：成绩明细（含与均分的差值，正值绿色、负值红色）

只看有成绩提交的作业（未提交的不计入）。Chart.js 通过 CDN 加载，需联网（仅首次加载）。

---

## 12. 数据备份与迁移

### 备份

```bash
# 直接复制数据库文件
cp data/database.sqlite data/database_backup_$(date +%Y%m%d).sqlite
```

### 迁移到另一台电脑

1. 复制整个项目文件夹到新电脑
2. 确保 `data/database.sqlite` 文件一并复制
3. 在新电脑上启动即可

> 打包版本的数据文件在可执行程序同级 `data/` 目录下。

---

## 13. 常见问题

### Q: 启动后浏览器打不开 http://localhost:3000？

**A**: 检查终端是否显示「🚀 学员管理系统已启动」，确认端口 3000 未被其他程序占用。可修改 `app.py` 最后一行的端口号。

### Q: 页面样式错乱或按钮不工作？

**A**: 按 Ctrl+F5 强制刷新浏览器缓存。

### Q: 如何重置所有数据？

**A**: 删除 `data/database.sqlite`，重新运行 `python3 seed.py` 生成示例数据。

### Q: 误删了数据能恢复吗？

**A**: 如果有备份，复制备份文件覆盖 `data/database.sqlite`。SQLite 数据库删除后无法恢复，建议定期备份。

### Q: 如何修改每页显示的学员数量？

**A**: 编辑 `app.py` 第 543 行，修改 `per_page = 15` 的数值。

### Q: 学员的"级"和"届"是什么关系？

**A**: 级 = 入学年份（如 26 级 = 2026 年入学），届 = 毕业年份（如 29 届 = 2029 年毕业）。系统自动用"级次 + 3"计算毕业届次。

---

## 14. 跨平台部署（Windows / Linux）

### 当前打包版本的局限

`dist/理学优课学员管理系统` 是在 **macOS arm64** 上打包的二进制文件，**无法直接在 Windows 或 Linux 上运行**。

### 方案一：源码运行（推荐，全平台通用）

只要目标电脑安装了 Python 3.9+，直接复制源码即可运行：

```bash
# 1. 安装 Python（python.org 下载安装包）
# 2. 复制以下文件到目标电脑：
#    - app.py
#    - database.py
#    - seed.py（可选）
#    - static/  文件夹
#    - templates/ 文件夹
#    - data/ 文件夹（如果已有数据）

# 3. 运行
python3 app.py
# 或 Windows: python app.py

# 4. 浏览器打开 http://localhost:3000
```

**不需要 pip install 任何东西**——系统 100% 依赖 Python 标准库。

### 方案二：在 Windows 上重新打包

在 Windows 电脑上安装 PyInstaller 后执行：

```bash
# 安装 PyInstaller
pip install pyinstaller

# 打包
pyinstaller student-system.spec --clean

# 输出在 dist/ 目录下
# dist/理学优课学员管理系统.exe
```

打包后的 `.exe` 文件可以分发给其他 Windows 用户，**无需安装 Python**。

### 方案三：局域网共享

如果团队内多台电脑在同一局域网，可以在一台电脑上启动服务，其他人通过 IP 访问：

```bash
# 启动服务器的那台电脑查看 IP
# macOS/Linux: ifconfig | grep inet
# Windows: ipconfig

# 假设服务器 IP 为 192.168.1.100
# 其他人浏览器访问：http://192.168.1.100:3000
```

> ⚠️ 此方式仅适合小团队局域网使用，不适合公网暴露。

### 各平台兼容性总结

| 平台 | 源码运行 | 打包版 | 操作 |
|------|---------|--------|------|
| macOS | ✅ | ✅ 已有 | python3 app.py |
| Windows | ✅ | 需重新打包 | python app.py |
| Linux | ✅ | 需重新打包 | python3 app.py |
| 局域网共享 | ✅ | ✅ | 修改 IP 访问 |

---

> 如有问题，参考 `PROJECT.md` 了解技术细节。
