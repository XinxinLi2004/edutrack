# 编程任务提示词：Eduflow 学员管理系统 — 安全与一致性缺陷修复

> 本提示词自包含，可直接交给另一个编程 Agent 执行，无需额外上下文。
> 执行前请先 `Read` 相关文件确认现状，再动手。不要凭记忆猜测。

---

## 0. 项目背景与约束

- **项目**：Eduflow 学员管理系统（教培行业，本地离线版）
- **技术栈**：纯 Python 标准库（`http.server` + `sqlite3` + 自研模板引擎），**零第三方依赖**；前端为原生 JS/CSS；用 `package.json` 的 npm 脚本启动。
- **定位**：本地离线运行的单用户桌面/局域网应用，打包为 Windows 可执行与 zip 分发。
- **硬性约束（务必遵守）**：
  1. **保持零第三方运行时依赖**。只允许改动标准库范围内的代码，不要引入 `pip`/`npm` 包。
  2. **只修缺陷，不要重构或重写**。本任务目标是修复安全问题与数据一致性 Bug，不是重构架构。改动面越小越好。
  3. **保持现有代码风格**（函数式、中文注释、4 空格缩进）。新增代码与之对齐。
  4. **不要破坏已有正常功能**：静态资源（css/js/图片）必须仍能正常加载；正常学员 CRUD/缴费/考勤流程不受影响。
  5. 修改后必须**自测**：启动服务，验证静态资源可加载、关键页面可访问、无 500。

---

## 1. 必须完成项（阻塞级，🔴）

### 任务 A — 修复路径穿越漏洞（最高优先，数据泄露风险）
- **文件**：`app.py`
- **函数**：`serve_static(self, path)`（约第 468–495 行）
- **现状**：
  ```python
  def serve_static(self, path):
      """提供静态文件"""
      file_path = os.path.join(ASSETS_DIR, path.lstrip('/'))   # ⚠️ 基于项目根目录，且未按前缀限定
      if not os.path.exists(file_path) or not os.path.isfile(file_path):
          self.send_error(404)
          return
      ...
  ```
  `ASSETS_DIR` 在文件顶部定义为项目根目录（`os.path.dirname(os.path.abspath(__file__))`）。攻击者可用 `GET /static/../data/database.sqlite` 下载**含学员隐私（姓名/手机/家长电话/地址）的整个数据库**，或 `GET /static/../app.py` 下载源码。
- **要求**：
  1. 先 `Read` 路由注册代码，确认 `serve_static` 收到的 `path` 实际形态（是否带 `static/` 前缀）。
  2. 将静态文件根目录从 `ASSETS_DIR` 改为专门的 `STATIC_DIR`（文件顶部已定义：`STATIC_DIR = os.path.join(ASSETS_DIR, 'static')`）。
  3. 用 `os.path.realpath` 归一化后做 **containment 校验**，确保最终路径严格位于 `STATIC_DIR` 之内，越界一律 404。
- **参考修复片段**（请按实际 path 形态微调前缀处理）：
  ```python
  def serve_static(self, path):
      """提供静态文件，严格限定在 STATIC_DIR 内，防路径穿越"""
      rel = path
      for prefix in ('/static/', 'static/'):
          if rel.startswith(prefix):
              rel = rel[len(prefix):]
              break
      file_path = os.path.realpath(os.path.join(STATIC_DIR, rel.lstrip('/')))
      static_root = os.path.realpath(STATIC_DIR)
      if not file_path.startswith(static_root + os.sep) or not os.path.isfile(file_path):
          self.send_error(404)
          return
      # 以下 content_type 判断与文件读取/响应逻辑保持不变
      ...
  ```
- **验证**：启动服务后，确认正常资源（如 `/static/css/*.css`、`/static/js/*.js`）仍能加载；尝试 `curl http://127.0.0.1:PORT/static/../data/database.sqlite` 返回 404。

### 任务 B — 绑定回环地址（缩小暴露面，配合 A）
- **文件**：`app.py`
- **位置**：`run_server` 函数内，`ThreadingHTTPServer(('0.0.0.0', port), AppHandler)`（约第 1473 行）
- **改动**：将 `'0.0.0.0'` 改为 `'127.0.0.1'`（本地离线应用无需监听全部网卡，避免内网任意机器访问）。
- **注意**：若项目明确需要局域网内其他设备访问，请先停下来向用户确认，不要擅自保留 `0.0.0.0`。

### 任务 C — package.json 去除硬编码绝对路径（可移植性硬伤）
- **文件**：`package.json`
- **现状**（第 6–9 行）：
  ```json
  "scripts": {
    "start": "/Users/seikaya/.workbuddy/binaries/python/versions/3.13.12/bin/python3 app.py",
    "seed": "/Users/seikaya/.workbuddy/binaries/python/versions/3.13.12/bin/python3 seed.py"
  }
  ```
- **改动**：改为依赖系统 `python3`，分发后才能在任意机器运行：
  ```json
  "scripts": {
    "start": "python3 app.py",
    "seed": "python3 seed.py"
  }
  ```
- **要求**：若仓库 `README`/`使用说明.md` 未说明 Python 版本要求，请在其中补充「需要 Python 3.10+」。不要改动 `package.json` 其他字段。

### 任务 D — 统一毕业届次计算（数据一致性 Bug，⚠️ 有隐藏陷阱）
- **文件**：`database.py`
- **位置**：`migrate_db()` 内（约第 60–70 行）
- **现状**：
  ```python
  if not row['cohort'] and row['grade'] in GRADE_TO_COHORT_OFFSET:
      cohort = (current_year + GRADE_TO_COHORT_OFFSET[row['grade']]) % 100
      graduation = (cohort + 2) % 100          # ⚠️ 错误：应为 +3（高中三年）
      cursor.execute(
          'UPDATE students SET cohort = ?, graduation_year = ? WHERE id = ?',
          (f'{cohort:02d}', f'{graduation:02d}', row['id'])   # ⚠️ graduation 被当成整数格式化
      )
  ```
  同文件第 88 行已有正确函数 `compute_graduation_year(cohort)`，它返回的是**字符串** `f'{(c+3)%100:02d}'`（例如 `'29'`）。
- **⚠️ 关键陷阱**：`compute_graduation_year` 返回的是字符串，而原代码用 `f'{graduation:02d}'` 把它当整数格式化会抛 `ValueError`。**不要直接把 `graduation = compute_graduation_year(cohort)` 套进 `f'{graduation:02d}'`**。
- **正确改法**：
  ```python
  if not row['cohort'] and row['grade'] in GRADE_TO_COHORT_OFFSET:
      cohort = (current_year + GRADE_TO_COHORT_OFFSET[row['grade']]) % 100
      cohort_str = f'{cohort:02d}'
      graduation_str = compute_graduation_year(cohort)   # 已是 '29' 形式的字符串
      cursor.execute(
          'UPDATE students SET cohort = ?, graduation_year = ? WHERE id = ?',
          (cohort_str, graduation_str, row['id'])
      )
  ```
- **验证**：用 `python3 -c "from database import compute_graduation_year; print(compute_graduation_year(26))"` 确认返回 `'29'`；修改后运行 `migrate_db()` 或重启服务，新建/迁移学员的 `graduation_year` 应为级次 +3。

---

## 2. 强烈建议完成项（🟡，若时间允许一并做）

### 任务 E — 添加 `.gitignore` 并清理已入库的敏感/产物文件
- 在项目根目录新建 `.gitignore`，至少排除：
  ```
  # 构建产物与分发包
  build/
  dist/
  *.zip
  # 含学员 PII 的数据库（切勿入库）
  *.sqlite
  *.sqlite-journal
  data/*.sqlite
  # Python 缓存
  __pycache__/
  *.pyc
  ```
- **不要**用命令物理删除已提交的文件后再提交——若需要把 `.sqlite` 移出版本库，请用 `git rm --cached` 从跟踪中移除（若当前为 git 仓库）。若不确定 git 状态，先在回复中说明，不要执行破坏性 git 操作。

### 任务 F — 为 query 参数的 int 转换加保护（防 500）
- **文件**：`app.py`
- **问题**：多处直接 `int(query.get('page', ['1'])[0])`、`int(class_id)`、`int(student_id)` 等，非数字输入（如 `?page=abc`）会抛 `ValueError` 导致 500。
- **要求**：封装一个安全转换 helper（如 `_safe_int(value, default=1)`），在 `try/except ValueError` 中返回默认值，并替换上述裸 `int()` 调用。**只改会崩的位置**，不要顺手重写其他逻辑。

---

## 3. 可选 / 长期项（🟢，本次非必须，可标注为后续 TODO）
- 为 `database.py` 的纯函数（`compute_graduation_year`、`_to_int`/`_to_float`、CSV 解析）补 `tests/` 单测（当前零测试）。
- 在 `README` 明确标注「当前无鉴权，禁止在公网部署」（`users` 表有密码字段但无登录逻辑）。
- 将 `app.py`/`database.py` 按业务域拆分模块（结构优化，非紧急）。

---

## 4. 验收标准（完成后必须逐项确认）
1. `serve_static` 对正常静态资源返回 200，对 `../` 穿越请求返回 404，且无法下载 `*.sqlite` 与 `*.py` 源码。
2. 服务仅监听 `127.0.0.1`（除非用户另行确认需要局域网）。
3. `npm start`（或 `python3 app.py`）在**干净环境**下无需修改即可启动（路径已去硬编码）。
4. 学员 `graduation_year` 计算为 `cohort + 3`；`migrate_db` 不再抛 `ValueError`。
5. 全站正常功能（列表/新增/编辑/删除/缴费/考勤）无回归。
6. 改动已通过 `python3 -m py_compile app.py database.py` 语法检查。

---

## 5. 完成后输出要求
请向用户回复：
- 逐项说明完成了哪些任务（A–F 哪些做了、哪些未做及原因）；
- 每个改动的具体文件与行号；
- 自测方式与结果（尤其是路径穿越的 404 验证、毕业届计算结果）；
- 遗留的 TODO 与建议的下一步。

不要提交 git（除非用户明确要求）。保持改动最小、可追溯。
