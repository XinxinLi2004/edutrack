# 学员管理系统 v2.0 升级概览

## 本次完成内容

针对现有系统的 7 项问题与改进需求，已完成全部修复与功能开发：

### 1. 模板渲染 Bug 修复 ✅
- 重写了模板引擎，完整支持 `if/elif/else`、`for`、`include` 嵌套
- 页面不再显示 Python 模板源码

### 2. 新增操作报错修复 ✅
- 为所有新增/编辑操作增加必填字段校验
- 为数值字段增加类型转换，避免空字符串导致数据库错误
- 异常时返回友好错误页面而非崩溃

### 3. 学员批量操作 ✅
- 新增 `/students/batch` 批量操作页面
- 支持批量加入班级、批量修改年级/级次/状态/学校
- 支持 CSV 批量导入学员，带预览与确认机制

### 4. 线上线下同步与请假机制 ✅
- 课程新增“上课方式”：线下 / 线上 / 线上线下同步
- 考勤状态新增“线上参与”
- 考勤记录区分“参与方式”（线下/线上）

### 5. 学员按“级”管理 ✅
- 学员与班级新增“级次”字段（如 26 级）
- 自动计算毕业届次（26 级 → 28 届）
- 保留“当前年级”字段，支持学年升级而不丢失历史数据

### 6. 成绩管理优化 ✅
- 成绩列表按班级分组展示
- 支持 CSV 格式成绩导入与导出（Excel 可直接打开）
- 作业提交统计展示提交/迟交/未交数量

### 7. 打包分发 ✅
- 使用 PyInstaller 打包为独立可执行程序
- 输出：`dist/理学优课学员管理系统`
- 双击或在终端运行即可启动，无需 Python 环境

---

## 技术要点

- **零外部依赖**：继续使用 Python 标准库 + SQLite
- **打包策略**：模板/静态资源打包进可执行文件，数据库文件放在可执行文件同级 `data/` 目录，便于读写和迁移
- **数据迁移**：复制 `data/database.sqlite` 即可备份或迁移全部数据

---

## 启动方式

### 开发模式
```bash
/Users/seikaya/.workbuddy/binaries/python/versions/3.13.12/bin/python3 seed.py
/Users/seikaya/.workbuddy/binaries/python/versions/3.13.12/bin/python3 app.py
```

### 打包版本
```bash
./dist/理学优课学员管理系统
```

## 后续修复（2026-07-04 下午）

针对李老师反馈的"无法正常使用"，进行了系统排查与修复：

### 发现的问题
1. **静态文件加载失败**：打包后的可执行程序中 `serve_static` 引用了未定义的 `BASE_DIR`，导致 CSS/JS 全部 500 错误，页面没有样式和交互
2. **成绩页面模板残留**：`grades/index.html` 使用了模板引擎不支持的 `for class_name, class_grades in grouped_grades.items()` 元组拆包，页面显示 `{% endfor %}` 源码
3. **`|length` 过滤器未支持**：班级人数、导入失败条数等使用 `|length` 的位置显示为空

### 修复内容
- 将 `serve_static` 中的 `BASE_DIR` 改为 `ASSETS_DIR`，确保开发模式和打包模式静态文件都能正确加载
- 将 `grouped_grades` 改为字典列表结构，模板使用 `for group in grouped_grades` + `group.class_name` / `group.grades`
- 扩展模板引擎 `VarNode`，支持 `|length` 过滤器
- 重新生成示例数据并重新打包，验证所有页面、新增操作、批量导入、成绩导出均正常

### 验证结果
- ✅ 开发服务器 `http://localhost:3000` 全部页面正常
- ✅ 打包可执行程序 `dist/理学优课学员管理系统` 全部页面、静态资源、新增/导入/导出功能正常

---

## 使用提示

- 打包版本在 macOS 上首次运行时，如果提示"无法打开"，请在 Finder 中右键点击选择**打开**，或在终端运行：`xattr -dr com.apple.quarantine "dist/理学优课学员管理系统"`
- 双击打包程序后，系统会在后台启动，浏览器访问 `http://localhost:3000` 即可使用（无桌面窗口）
- 数据文件在 `data/database.sqlite`，复制即可备份/迁移
